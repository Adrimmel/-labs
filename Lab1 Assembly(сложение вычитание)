.data #объявляются переменные, хранящие значения чисел. Здесь определены first и second, которые хранят значения 15 и 7 соответственно
first: .word 15
second: .word 7

sum: .word 0 #объявляются переменные sum и dif, которые будут использованы для хранения результатов суммы и разности чисел
dif: .word 0

.text
lw $t0, first #Загружается значение переменной num1 из памяти в регистр $t0 с помощью инструкции lw

lw $t1, second #то же самое 
sub $t2,$t0,$t1 #сложение
sw $t2, dif #Значение, хранящееся в регистре $t2, сохраняется в памяти по адресу переменной diff с помощью инструкции sw

lw $t4, first #повторяем то же с суммой
lw $t5, second
add $t6, $t5, $t4
sw $t6, sum

li $v0, 10 #Загружается значение 10 в регистр $v0 с помощью инструкции li.
10) Выполняется системный вызов (syscall) с использованием значения, хранящегося в регистре $v0, чтобы завершить программу
syscall
