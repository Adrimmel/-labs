#include <stdio.h>

int main() {
    float x;
    printf("Введите значение x: ");
    scanf("%f", &x);
    
    float result = 2 * x * x + x + 41;
    printf("Значение полинома при x = %.2f равно: %.2f\n", x, result);
    
    return 0;
}
